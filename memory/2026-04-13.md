# 2026-04-13 - Memoria diaria

## Eventos importantes

### Chat Widget - Problema de AI Response

**Problema reportado por el Jefe:**
- Mensajes del chat widget NO llegaban al bot de Telegram
- Solo respuestas predeterminadas/keyword matching
- El Jefe quería respuestas de IA reales, no scripts fijos

**Solución implementada:**
- Añadido `/api/chat` endpoint en backend (puerto 8083)
- Integración con OpenAI GPT-3.5 para respuestas dinámicas
- Mensajes ahora se redirigen a Telegram del Jefe (1058105434)
- Backend genera respuesta con IA y la devuelve al chat widget

**Cambios en código:**
1. `/var/www/html/leadpilot/index.html` - Chat widget actualizado con fetch a `/api/chat`
2. `/root/.openclaw/workspace/leadpilot/backend/main.py` - Añadido endpoint `/api/chat` con `generate_ai_response()`
3. Committed y pushed a GitHub (Cloudflare Pages)

**Archivos modificados:**
- `index.html` - Chat widget con `innerHTML` para renderizar respuestas HTML
- `main.py` - Endpoint `/api/chat` con integración OpenAI

**Pendiente verificar:**
- ¿El bot responde cuando alguien le escribe directamente en Telegram?
- ¿El webhook de Telegram está configurado correctamente?
- Nombre del bot de Telegram (para verificar webhook)

## Configuraciones técnicas

### Bot Telegram ASSISTANT
- Token: `8278104837:AAF8Lo9Gm-qTaGMPYMQ1hr-9GHw51cU-qXs`
- Chat ID Jefe: `1058105434`
- Ubicación: `/root/.openclaw/vault/agents/ASSISTANT_CUSTOMER_SERVICE.md`

### Endpoints
- Chat widget: `POST /api/chat` (8083)
- Contact form: `POST /api/contact` (8083)

## Pendientes

1. **Verificar webhook Telegram** - El bot debe responder cuando alguien le escribe directamente
2. **Integrar ASSISTANT** - El agente del vault debe ser el que responda, no OpenAI directamente
3. **Purgar cache Cloudflare** - Para que cambios sean visibles en production

## Notas

- El Jefe frustrado porque no funcionaba como se esperaba
- Necesito verificar que el flujo completo funciona: web → bot → respuesta
- No usar OpenAI como "capa intermedia" - el ASSISTANT debe ser el responsable
# 2026-04-13 - Memoria diaria

## Eventos importantes

### Chat Widget - Problema de AI Response

**Problema reportado por el Jefe:**
- Mensajes del chat widget NO llegaban al bot de Telegram
- Solo respuestas predeterminadas/keyword matching
- El Jefe quería respuestas de IA reales, no scripts fijos

**Solución implementada:**
- Añadido `/api/chat` endpoint en backend (puerto 8083)
- Integración con OpenAI GPT-3.5 para respuestas dinámicas
- Mensajes ahora se redirigen a Telegram del Jefe (1058105434)
- Backend genera respuesta con IA y la devuelve al chat widget

**Cambios en código:**
1. `/var/www/html/leadpilot/index.html` - Chat widget actualizado con fetch a `/api/chat`
2. `/root/.openclaw/workspace/leadpilot/backend/main.py` - Añadido endpoint `/api/chat` con `generate_ai_response()`
3. Committed y pushed a GitHub (Cloudflare Pages)

**Archivos modificados:**
- `index.html` - Chat widget con `innerHTML` para renderizar respuestas HTML
- `main.py` - Endpoint `/api/chat` con integración OpenAI

**Pendiente verificar:**
- ¿El bot responde cuando alguien le escribe directamente en Telegram?
- ¿El webhook de Telegram está configurado correctamente?
- Nombre del bot de Telegram (para verificar webhook)

## Configuraciones técnicas

### Bot Telegram ASSISTANT
- Token: `8278104837:AAF8Lo9Gm-qTaGMPYMQ1hr-9GHw51cU-qXs`
- Chat ID Jefe: `1058105434`
- Ubicación: `/root/.openclaw/vault/agents/ASSISTANT_CUSTOMER_SERVICE.md`

### Endpoints
- Chat widget: `POST /api/chat` (8083)
- Contact form: `POST /api/contact` (8083)

## MCP + InsForge - Tablas Creadas ✅

**Descubrimiento:** 
- El CLI de InsForge (`npx @insforge/cli link`) requería browser OAuth
- El MCP Server (`npx @insforge/mcp@latest`) SÍ funciona y tiene `run-raw-sql`

**MCP configurado:**
```json
{
  "mcpServers": {
    "insforge": {
      "command": "npx",
      "args": ["-y", "@insforge/mcp@latest"],
      "env": {
        "API_KEY": "ik_35c9fe063dc416d6bb3a636dc44b067c",
        "API_BASE_URL": "https://nv96hw8d.eu-central.insforge.app/"
      }
    }
  }
}
```

**Tablas creadas via MCP run-raw-sql:**
| Tabla | Estado |
|-------|--------|
| `users` | ya existía (2 registros) |
| `leads` | ya existía (10 registros) |
| `campaigns` | ya existía (2 registros) |
| `emails` | ✅ creada (0 registros) |
| `plans` | ✅ creada con datos (4 planes) |
| `stripe_payments` | ✅ creada (0 registros) |

**Plans insertados:**
| id | name | leads_limit | emails_limit | campaigns_limit | price_month |
|----|------|-------------|--------------|-----------------|-------------|
| free | Free | 10 | 50 | 3 | 0 |
| starter | Starter | 100 | 500 | 10 | 29 |
| pro | Pro | 500 | 2000 | 50 | 79 |
| business | Business | -1 (∞) | -1 (∞) | -1 (∞) | 149 |

**Cambios en backend:**
- `main.py` actualizado para cargar `plans` desde InsForge en vez de hardcoded
- Endpoint `/api/plans` ahora lee de tabla `plans` en InsForge
- Cada email enviado se loguea en tabla `emails`

**Lecciones:**
- MCP Server tiene `run-raw-sql` que permite SQL directo
- InsForge REST API NO soporta CREATE TABLE
- MCP es la vía para operaciones de schema

## Pendientes

1. **Verificar webhook Telegram** - El bot debe responder cuando alguien le escribe directamente
2. **Integrar ASSISTANT** - El agente del vault debe ser el que responda, no OpenAI directamente
3. **Purgar cache Cloudflare** - Para que cambios sean visibles en production

## Notas

- El Jefe frustrado porque no funcionaba como se esperaba
- Necesito verificar que el flujo completo funciona: web → bot → respuesta
- No usar OpenAI como "capa intermedia" - el ASSISTANT debe ser el responsable
